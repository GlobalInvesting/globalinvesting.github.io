name: Update Myfxbook Sentiment
# PUBLIC STUB — schedule trigger only.
on:
  schedule:
    - cron: '20 * * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: update-myfxbook-sentiment
  cancel-in-progress: false

jobs:
  call:
    uses: GlobalInvesting/globalinvesting-scripts/.github/workflows/update-myfxbook-sentiment.yml@main
    secrets: inherit
