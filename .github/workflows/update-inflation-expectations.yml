name: Update Inflation Expectations
# PUBLIC STUB — schedule trigger only.
on:
  schedule:
    - cron: '0 7 * * 3'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: update-inflation-expectations
  cancel-in-progress: false

jobs:
  call:
    uses: GlobalInvesting/globalinvesting-scripts/.github/workflows/update-inflation-expectations.yml@main
    secrets: inherit
