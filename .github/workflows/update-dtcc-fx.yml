name: Update DTCC FX OTC Volume
# PUBLIC STUB — schedule trigger only.
on:
  schedule:
    - cron: '0 14 * * 1-5'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: update-dtcc-fx
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  call:
    uses: GlobalInvesting/globalinvesting-scripts/.github/workflows/update-dtcc-fx.yml@main
    secrets: inherit
