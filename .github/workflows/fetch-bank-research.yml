name: Fetch Bank Research
# PUBLIC STUB — schedule trigger only.
on:
  schedule:
    - cron: '15 */4 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: fetch-bank-research
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  call:
    uses: GlobalInvesting/globalinvesting-scripts/.github/workflows/fetch-bank-research.yml@main
    secrets: inherit
